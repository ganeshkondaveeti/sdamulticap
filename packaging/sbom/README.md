# SBOM Baseline (§5 Phase 0 exit criterion, R6)

Reference SBOMs regenerated by CI on every PR via `sbom.yml`. Any drift against the checked-in baselines under `ref/` fails the build.

## Regenerate locally

```bash
./generate.sh
```

Produces:

- `ref/multicap.python.cdx.json` — CycloneDX from `cyclonedx-py` over the Python distribution
- `ref/correlator_rs.rust.cdx.json` — CycloneDX from `cargo-cyclonedx` over the pyo3 crate
- `ref/multicap.combined.cdx.json` — merged view for release attestation (§15)
